import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {},
  mapView: {},
  controlContainer: {
    position: 'absolute',
    top: 10,
    right: 10,
    flexDirection: 'column'
  },
  controlItem: {
    marginBottom: 10
  }
});
