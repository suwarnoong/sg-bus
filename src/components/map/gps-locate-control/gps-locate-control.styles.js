import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    opacity: 0.8,
    borderWidth: 2,
    shadowColor: '#999999',
    shadowOffset: { width: 0, height: 3 },
    shadowOpacity: 0.8,
    shadowRadius: 4
  }
});
